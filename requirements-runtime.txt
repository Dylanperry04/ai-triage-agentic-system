# Production runtime for the active FastAPI backend and built React UI.
# Python 3.11 is the deployment target. Heavy model-training candidates and the
# retired Streamlit UI live in their own manifests and must not enter the web ZIP.

# Web framework. Starlette and multipart are pinned explicitly to security-fixed
# releases because this application exposes a multipart upload route.
fastapi==0.140.0
starlette==1.3.1
python-multipart==0.0.32
uvicorn==0.30.1
gunicorn==23.0.0

# Active UHL data/model serving.
pandas==2.2.2
numpy==1.26.4
pydantic==2.10.6
scikit-learn==1.5.1
joblib==1.4.2
catboost==1.2.5

# AutoGen explanation runtime and Azure OpenAI client.
openai==2.41.0
autogen-agentchat==0.7.5
autogen-core==0.7.5
autogen-ext[openai]==0.7.5

# Environment, serialisation, HTTP, and optional governance evidence logging.
python-dotenv==1.2.2
orjson==3.11.6
requests==2.33.0
httpx==0.27.2
wandb>=0.28.1,<1.0
protobuf==5.29.6

# Durable notification publication from the web app.
azure-identity==1.25.3
azure-data-tables==12.7.0
azure-servicebus==7.14.3
phonenumbers==9.0.36

# Existing utility imported by retained application workflows.
tqdm==4.66.4
