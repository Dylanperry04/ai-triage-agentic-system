# Data folder

Current active dataset phase:

```text
data/raw/kaggle_ktas/data.csv
```

Processed files are written to:

```text
data/processed/
```

Trained research models are written to:

```text
data/models/
```

MIMIC-IV-ED paths are preserved in configuration for the later approved-data phase, but this package is currently configured for Kaggle KTAS.
