# ── Test / development dependencies ─────────────────────────────────────────
# Install on top of requirements.txt: pip install -r requirements.txt -r requirements-dev.txt
pytest==9.0.3
pytest-cov==5.0.0
# Retained source-history tests exercise the retired Streamlit presentation.
# This is test/development-only and is intentionally absent from the active
# FastAPI/React deployment manifest.
streamlit==1.54.0
