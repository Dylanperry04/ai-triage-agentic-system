# Active FastAPI/React application runtime.
# Keep this thin indirection because Azure/Oryx and existing local setup commands
# conventionally install requirements.txt. Training and retired UI dependencies
# are intentionally not part of the deployed web service.
-r requirements-runtime.txt
